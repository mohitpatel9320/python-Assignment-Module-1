# Example of checking the type of a variable dynamically using type()

# Define a variable
my_variable = 42

# Check the type of the variable
variable_type = type(my_variable)

# Print the type
print(f"The type of the variable is: {variable_type}")