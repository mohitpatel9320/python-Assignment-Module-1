# Program to print a string from the last character

# Input string from the user
input_string = input("Enter a string: ")

# Print the string from the last character
reversed_string = input_string[::-1]
print("String from the last character:", reversed_string)